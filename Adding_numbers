# Prompt the user to enter the first number and store it in variable 'a'
a = int(input("Enter the first number: "))

# Prompt the user to enter the second number and store it in variable 'b'
b = int(input("Enter the second number: "))

# Calculate the sum of 'a' and 'b' and store it in variable 'sum'
sum = a + b

# Update the value of 'a' by adding 'b' to it
a = a + b

# Display the sum of 'a' and 'b' along with their values
print("The sum of", a, "and", b, "is", sum)

# Display the new value of 'a'
print("The new value of a is", a)

# Update the value of 'b' by adding the original value of 'b' to it
b = b + b

# Display the current value of 'b'
print("The current value of b is", b)

# Calculate the sum of the updated 'a' and 'b' and display it
sum = a + b
print("The sum of", a, "and", b, "is", sum)
